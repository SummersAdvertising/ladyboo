<link rel="stylesheet" href="/css/pageProdPickup.css">
<link rel="stylesheet" href="/css/prodList.css">
<script src="/js/side.js"></script> 

<% content_for :meta_area do %>

  <meta property="og:title" content="<%= "#{@topic_collection.name} #{@topic.name}" %>">
  <% if @topic.galleries.where(type: 'TopicBannerGallery').first %>
    <meta property="og:image" content="<%= image_url(@topic.galleries.where(type: 'TopicBannerGallery').first.attachment.url) %>">
  <% end %>
  <meta property="og:url" content="<%= topic_collection_url(@topic_collection) %>">
  
  
<% end %>

<% content_for :breadcrumb do %>
  <%= link_to 'Home', root_path, title: '首頁' %> &nbsp; > &nbsp; Collection
  &nbsp; > &nbsp; <%= "#{@topic_collection.name}" %>
<% end %>

<% if @topic.galleries.select{ |v| v['type'] == 'TopicBannerGallery' }.first  %>
  <div id="prodTop"><!-- Start #prodTop 上方橫福 --> 
    <div class="container">
      <div class="col-md-12">
        <div class="row">
          <img id="bannerImg" src=<%= "#{@topic.galleries.select{ |v| v['type'] == 'TopicBannerGallery' }.first.attachment.url}" %>>
          <img id="bannerFrame" src="/images/frame_bannerPickup.png">
          <div class="fb-like" data-href="<%= request.original_url %>" data-layout="button_count" data-action="like" data-show-faces="false" data-share="false"></div>
        </div>
      </div>
    </div>
  </div><!-- End #prodTop --> 
<% end %>
    

<div class="xlg-margin hidden-xs"></div><!-- Space 間隔 -->
<div class="container"><!-- Start .container -->
  
  <%= render 'aside' %>
  
  <%= render 'product_grid' %>

</div><!-- End .container -->